#ifndef BLITSPRITE_HPP
#define BLITSPRITE_HPP

//~ void apply_surface( int x, int y, SDL_Surface* source, SDL_Surface* destination, SDL_Rect* clip );
void blitSprite( int x, int y, SDL_Surface* src, SDL_Surface* dst, SDL_Rect* srcOffset )

#endif
